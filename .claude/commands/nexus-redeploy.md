Read docs/DEPLOYMENT-RUNBOOK.md, then use `npm run deploy:service` (add `-- --frontend` for frontend changes). Verify the actual installation and service scope. Deployment requires user authorization.
