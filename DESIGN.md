# DESIGN

目标：给 Nexus 的日常 UI 变更提供最小但硬的设计约束，避免每个 feature 都重新争论一次层级、状态和按钮语义。

## 1. 设计原则

- 单用户、工具型产品优先。不要为了“像 SaaS”而引入重装饰和伪仪表盘。
- 当前工作上下文优先。项目、频道、历史、文件都必须明确属于哪个工作区。
- 页面以“可操作”优先，而不是“可观赏”优先。任何视觉强化都不能挡住主操作流。
- 复用现有语汇，不新造第二套视觉系统。

## 2. 面板语汇

- `workspace shell`
  - 主工作区，承载终端与当前操作。
- `manager panel`
  - 会话/项目/频道管理面板。
  - 用于切换、创建、删除、重命名。
- `memory panel`
  - 用于历史、记录、可回溯信息。
  - 例如 Codex 历史会话。
- `utility panel`
  - 文件、设置、辅助工具。

规则：

- 同一屏内不要混淆 `manager panel` 和 `memory panel` 语义。
- 历史类内容默认放在当前工作区内部，而不是和工作区列表并列成全局一级导航。

## 3. 状态语义

- `loading`
  - 保留骨架，不整屏抖动。
- `empty`
  - 说明当前范围内没有内容。
  - 必须给一个主动作。
- `warning`
  - 非阻塞。
  - 列表或主内容仍可继续使用。
- `error`
  - 仅阻塞当前失败区域，不清空上下文标题。
  - 必须有恢复动作。
- `success`
  - 默认少提示，除非用户否则不弹 celebratory toast。

## 4. 按钮等级

- `primary`
  - 每个局部区域最多一个。
  - 用于当前最主要动作，例如创建、重试、继续。
- `secondary`
  - 次要但常用动作，例如刷新、返回、查看更多。
- `danger`
  - 删除、清空、不可逆操作。
  - 必须显式文案，不靠纯图标表达。
- `quiet`
  - 展开详情、复制路径、轻量切换。

规则：

- `warning` 区域只放 `secondary` 或 `quiet` 动作。
- `error` 区域才允许 `primary + secondary` 并存。
- 同一行不要放两个竞争性的 `primary`。

## 5. 层级与入口

- 桌面端：
  - 一级：工作区/主内容结构。
  - 二级：当前工作区内部的标签、历史、辅助面板。
- 移动端：
  - 优先保留显式返回路径。
  - 系统返回只能是辅助路径，不能是唯一返回方式。

规则：

- “属于当前工作区”的能力，不要做成和工作区列表同级的全局 tab。
- 新入口先问自己：这是切换上下文，还是查看当前上下文里的附加信息？

## 6. 文案规则

- 标题先说范围，再说对象。
  - 例：`Codex 历史会话 · <Project>`
- warning 和 error 文案先解释“这次发生了什么”，再暗示“系统整体并未失效”。
- 避免抽象词堆叠，例如“资源异常”“状态错误”“模块失败”。

## 7. 新功能检查单

- 这个入口属于全局，还是属于当前工作区？
- loading / empty / warning / error 是否都定义了？
- 是否只用了现有按钮等级？
- 是否给了移动端显式返回路径？
- 是否复用了现有 `nexus-*` 色彩和面板层级？
